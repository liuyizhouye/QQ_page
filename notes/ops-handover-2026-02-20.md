# 运维续办交接文档（2026-02-20，已归档）

更新时间：2026-03-12

## 当前状态

本文记录的是 2026-02-20 的中间状态，后续关键事项已经完成：

- 域名已从 GoDaddy 转入阿里云
- ICP 备案已完成
- DNS 已切到阿里云云解析
- 主站与 API 已统一部署到阿里云 ECS

当前请优先查看项目根目录的 `AGENTS.md`，那里才是最新的架构和后续待办。

## 这份文档为什么保留

它仍然有历史价值，用于解释当时为什么会有：

- GitHub Pages 前端
- GoDaddy NS
- API 外网访问异常
- 域名转入与备案这条迁移路线

## 2026-02-20 时的原始判断

- 当时前端仍在 GitHub Pages
- 当时域名仍在 GoDaddy
- 当时阿里云备案尚未开始
- 当时判断方向是先转入阿里云，再做 ICP 备案，再恢复 API 公网访问

这个判断已经被后续执行验证为正确，但执行结果已发生变化，因此不应继续把本文当作操作手册。

## 已完成结果（2026-03-12）

- `hanbaodoudou.com` -> ECS 静态站
- `www.hanbaodoudou.com` -> 301 到主域名
- `api.hanbaodoudou.com` -> ECS API
- 域名 NS -> `dns27.hichina.com` / `dns28.hichina.com`

## 后续工作

后续重点不再是域名/备案，而是：

1. 做最小监控、备份与恢复验证
2. 收敛前端硬编码密码等安全债务
