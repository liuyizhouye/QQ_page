# 服务端口
PORT=8080

# 管理员 API 密钥 (可配置多个，逗号分隔，便于轮换)
ADMIN_API_KEYS=replace_this_with_a_secure_key

# 数据库文件路径
DATABASE_FILE=database/qq_story.db

# 上传文件存储路径
# 如果要挂载 NAS，请修改这里为挂载点，例如 /mnt/nas_uploads
UPLOAD_DIR=uploads

# 静态资源基础 URL (CDN / NAS 直连加速)
# 如果留空，则默认使用当前服务器的 /uploads 路径 (走 ECS 流量)
# 如果你有 NAS 的公网域名 (如 https://media.yoursite.com)，填入这里可加速图片视频加载
MEDIA_BASE_URL=

# 允许跨域的域名 (用逗号分隔，生产环境建议列出具体站点)
ALLOWED_ORIGINS=https://hanbaodoudou.com,https://api.hanbaodoudou.com

# 上传大小限制 (MB)
MAX_UPLOAD_SIZE_MB=100

# 速率限制
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX_REQUESTS=300

