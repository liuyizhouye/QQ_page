[common]
bind_addr = 0.0.0.0
bind_port = 7000
token = change-this-to-a-long-random-string
tcp_mux = true
log_file = /var/log/frp/frps.log
log_level = info
log_max_days = 3

# 将后端服务端口仅暴露在本机，所有公网流量通过 Caddy 转发。
proxy_bind_addr = 127.0.0.1
